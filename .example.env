SANDBOX_WORKSPACE=/path/to/your/sandbox-workspace

# API keys and secrets (optional) — loaded into the container via env_file.
# See the Security Warning in README.md before adding secrets here.
# GITHUB_TOKEN=your_github_token_here
